# NightGuard 改进计划 1.0

## 文档信息
- **版本**: 1.0
- **创建日期**: 2026-04-28
- **目标**: 提升美国市场适配度，完善核心功能

---

## 一、设计原则

### 1.1 核心原则
1. **单一职责**: 一个功能一个模块，高内聚低耦合
2. **代码复用**: 优先复用现有模块，符合"三次法则"
3. **渐进重构**: 标记废弃 → 验证无影响 → 删除
4. **风格一致**: 保持现有 Indigo/Purple 配色和毛玻璃设计风格
5. **继承优先**: 优先扩展现有类而非硬编码

### 1.2 文件命名规范
```
Services/     - 业务逻辑服务
Models/       - 数据模型
Views/        - 视图组件
  /Components - 可复用UI组件
  /[Feature]  - 功能模块视图
ViewModels/   - 视图模型
Extensions/   - 扩展
Protocols/    - 协议定义
```

---

## 二、模块优化方案

### 模块 1: HealthKit 集成服务

#### 需求描述
集成 Apple HealthKit，将睡眠数据同步到系统健康应用，同时读取用户的睡眠目标。

#### 生成规则

**1.1 新建文件: `Services/HealthKitService.swift`**
```swift
import HealthKit
import Foundation

/// HealthKit 睡眠数据管理服务
/// 职责: 读写 HealthKit 睡眠数据，处理权限请求
final class HealthKitService: ObservableObject {
    
    static let shared = HealthKitService()
    
    @Published var isAuthorized = false
    @Published var sleepGoalHours: Double = 8.0
    
    private let healthStore = HKHealthStore()
    private let sleepType = HKObjectType.categoryType(forIdentifier: .sleepAnalysis)!
    
    // MARK: - 权限管理
    
    /// 请求 HealthKit 授权
    /// - Returns: 是否获得授权
    func requestAuthorization() async throws -> Bool {
        guard HKHealthStore.isHealthDataAvailable() else {
            return false
        }
        
        try await healthStore.requestAuthorization(
            toShare: [sleepType],
            read: [sleepType, HKObjectType.quantityType(forIdentifier: .sleepGoal)!]
        )
        
        let status = healthStore.authorizationStatus(for: sleepType)
        isAuthorized = status == .sharingAuthorized
        return isAuthorized
    }
    
    // MARK: - 数据写入
    
    /// 保存睡眠记录到 HealthKit
    /// - Parameters:
    ///   - startDate: 入睡时间
    ///   - endDate: 醒来时间
    ///   - source: 数据来源标识
    func saveSleepAnalysis(startDate: Date, endDate: Date, source: String = "NightGuard") async throws {
        guard isAuthorized else { throw HealthKitError.notAuthorized }
        
        let sample = HKCategorySample(
            type: sleepType,
            value: HKCategoryValueSleepAnalysis.asleep.rawValue,
            start: startDate,
            end: endDate,
            metadata: [HKMetadataKeyDeviceManufacturerName: source]
        )
        
        try await healthStore.save(sample)
    }
    
    // MARK: - 数据读取
    
    /// 获取最近7天的睡眠数据
    /// - Returns: 睡眠样本数组
    func fetchRecentSleepData(days: Int = 7) async throws -> [HKCategorySample] {
        guard isAuthorized else { throw HealthKitError.notAuthorized }
        
        let endDate = Date()
        let startDate = Calendar.current.date(byAdding: .day, value: -days, to: endDate)!
        
        let predicate = HKQuery.predicateForSamples(withStart: startDate, end: endDate, options: .strictStartDate)
        let sortDescriptor = NSSortDescriptor(key: HKSampleSortIdentifierStartDate, ascending: false)
        
        return try await withCheckedThrowingContinuation { continuation in
            let query = HKSampleQuery(
                sampleType: sleepType,
                predicate: predicate,
                limit: HKObjectQueryNoLimit,
                sortDescriptors: [sortDescriptor]
            ) { _, samples, error in
                if let error = error {
                    continuation.resume(throwing: error)
                } else {
                    continuation.resume(returning: (samples as? [HKCategorySample]) ?? [])
                }
            }
            healthStore.execute(query)
        }
    }
}

enum HealthKitError: Error {
    case notAuthorized
    case dataNotAvailable
    case saveFailed
}
```

**1.2 修改文件: `Models/SleepRecord.swift`**
```swift
import Foundation
import SwiftData

@Model
final class SleepRecord {
    var id: UUID
    var date: Date
    var bedtime: Date
    var wakeTime: Date
    var durationSeconds: Double
    var bypassAttemptCount: Int
    var wasHardLock: Bool
    
    // MARK: - 新增字段
    var isSyncedToHealthKit: Bool  // 同步状态标记
    var healthKitUUID: String?     // HealthKit 样本标识
    var sleepQuality: Int?         // 用户自评睡眠质量 1-5
    
    init(date: Date, bedtime: Date, wakeTime: Date, durationSeconds: Double, 
         bypassAttemptCount: Int = 0, wasHardLock: Bool = true,
         sleepQuality: Int? = nil) {
        self.id = UUID()
        self.date = date
        self.bedtime = bedtime
        self.wakeTime = wakeTime
        self.durationSeconds = durationSeconds
        self.bypassAttemptCount = bypassAttemptCount
        self.wasHardLock = wasHardLock
        self.isSyncedToHealthKit = false
        self.healthKitUUID = nil
        self.sleepQuality = sleepQuality
    }
}
```

**1.3 修改文件: `Views/Stats/StatsView.swift`**
```swift
// 在 StatCard 区域添加 HealthKit 同步状态
Section("HealthKit") {
    if healthKitService.isAuthorized {
        HStack {
            Image(systemName: "heart.fill")
                .foregroundStyle(.red)
            Text("Syncing to Health")
            Spacer()
            Image(systemName: "checkmark.circle.fill")
                .foregroundStyle(.green)
        }
    } else {
        Button("Connect to Apple Health") {
            Task { await requestHealthKitAccess() }
        }
    }
}
```

#### 检验标准

**测试用例 1: 权限请求**
```swift
func testHealthKitAuthorization() async {
    let service = HealthKitService.shared
    let result = try? await service.requestAuthorization()
    assert(result == true || result == false) // 根据用户选择
}
```

**测试用例 2: 数据保存**
```swift
func testSaveSleepData() async {
    let start = Date().addingTimeInterval(-28800) // 8小时前
    let end = Date()
    
    do {
        try await HealthKitService.shared.saveSleepAnalysis(
            startDate: start, 
            endDate: end
        )
        // 验证: 在 Health App 中查看数据是否存在
    } catch {
        XCTFail("保存失败: \(error)")
    }
}
```

---

### 模块 2: Partner 功能完善

#### 需求描述
重构 Accountability Partner 功能，添加真实联系方式（电话/邮箱）、通知机制、分享功能。

#### 生成规则

**2.1 修改文件: `Models/AccountabilityPartner.swift`**
```swift
import Foundation
import SwiftData

/// 监督伙伴联系类型
enum ContactType: String, Codable, CaseIterable {
    case phone = "Phone"
    case email = "Email"
    case message = "Message"
    
    var icon: String {
        switch self {
        case .phone: return "phone.fill"
        case .email: return "envelope.fill"
        case .message: return "message.fill"
        }
    }
}

@Model
final class AccountabilityPartner {
    var id: UUID
    var name: String
    var contactIdentifier: String       // 电话或邮箱
    var contactType: ContactType
    var notifyOnBypassAttempt: Bool
    var notifyOnEmergencyOverride: Bool
    var notifyOnDailyReport: Bool       // 新增: 每日报告
    
    // MARK: - 新增字段
    var relationship: String?           // 关系: 朋友、家人、伴侣等
    var avatarData: Data?               // 头像数据
    var createdAt: Date
    var lastNotifiedAt: Date?
    
    init(name: String, 
         contactIdentifier: String, 
         contactType: ContactType = .message,
         relationship: String? = nil) {
        self.id = UUID()
        self.name = name
        self.contactIdentifier = contactIdentifier
        self.contactType = contactType
        self.notifyOnBypassAttempt = true
        self.notifyOnEmergencyOverride = true
        self.notifyOnDailyReport = false
        self.relationship = relationship
        self.createdAt = Date()
    }
    
    /// 验证联系方式格式
    var isValidContact: Bool {
        switch contactType {
        case .phone:
            // 基础电话格式验证
            let phoneRegex = "^\\+?[0-9\\s\\-\\(\\)]{10,}$"
            return NSPredicate(format: "SELF MATCHES %@", phoneRegex).evaluate(with: contactIdentifier)
        case .email:
            let emailRegex = "[A-Z0-9a-z._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}"
            return NSPredicate(format: "SELF MATCHES %@", emailRegex).evaluate(with: contactIdentifier)
        case .message:
            return !contactIdentifier.isEmpty
        }
    }
}
```

**2.2 新建文件: `Services/PartnerNotificationService.swift`**
```swift
import Foundation
import MessageUI
import SwiftData

/// 伙伴通知服务
/// 职责: 处理向监督伙伴发送通知的所有逻辑
final class PartnerNotificationService: ObservableObject {
    
    static let shared = PartnerNotificationService()
    
    @Published var canSendMessages = false
    @Published var canSendMail = false
    
    private let defaults = UserDefaults(suiteName: AppGroupConstants.suiteName)
    
    // MARK: - 通知类型
    
    enum NotificationType {
        case bypassAttempt(date: Date, appName: String?)
        case emergencyOverride(date: Date, reason: String?)
        case dailyReport(sleepData: SleepSummary)
        case streakMilestone(days: Int)
        
        var title: String {
            switch self {
            case .bypassAttempt: return "NightGuard Alert"
            case .emergencyOverride: return "Emergency Override Used"
            case .dailyReport: return "Daily Sleep Report"
            case .streakMilestone(let days): return "\(days) Day Streak! 🎉"
            }
        }
        
        func message(for userName: String) -> String {
            switch self {
            case .bypassAttempt(let date, let appName):
                let app = appName ?? "an app"
                return "\(userName) attempted to open \(app) at \(date.formatted(date: .omitted, time: .shortened)) but NightGuard blocked it. Send them some encouragement!"
            case .emergencyOverride(let date, let reason):
                let reasonText = reason.map { " Reason: \($0)" } ?? ""
                return "\(userName) used Emergency Override at \(date.formatted(date: .omitted, time: .shortened)).\(reasonText)"
            case .dailyReport(let data):
                return "\(userName)'s sleep last night: \(data.duration) hours. Quality: \(data.quality)/5."
            case .streakMilestone(let days):
                return "\(userName) has maintained their sleep schedule for \(days) days straight! 🌟"
            }
        }
    }
    
    struct SleepSummary {
        let duration: Double
        let quality: Int
        let bedtime: Date
        let wakeTime: Date
    }
    
    // MARK: - 公共方法
    
    /// 发送通知给所有启用的伙伴
    func notifyPartners(type: NotificationType, from context: ModelContext) {
        let descriptor = FetchDescriptor<AccountabilityPartner>()
        guard let partners = try? context.fetch(descriptor) else { return }
        
        let userName = defaults?.string(forKey: AppGroupKeys.userName) ?? "Your friend"
        
        for partner in partners where shouldNotify(partner: partner, type: type) {
            sendNotification(to: partner, type: type, userName: userName)
        }
    }
    
    // MARK: - 私有方法
    
    private func shouldNotify(partner: AccountabilityPartner, type: NotificationType) -> Bool {
        switch type {
        case .bypassAttempt:
            return partner.notifyOnBypassAttempt
        case .emergencyOverride:
            return partner.notifyOnEmergencyOverride
        case .dailyReport:
            return partner.notifyOnDailyReport
        case .streakMilestone:
            return true // 里程碑总是通知
        }
    }
    
    private func sendNotification(to partner: AccountabilityPartner, type: NotificationType, userName: String) {
        // 实际发送逻辑（消息/邮件/短信）
        // 这里需要集成 MFMessageComposeViewController 或邮件服务
        // 由于扩展限制，实际发送可能需要通过主应用进行
        
        // 记录通知时间
        partner.lastNotifiedAt = Date()
    }
}
```

**2.3 新建文件: `Views/Components/PartnerCardView.swift`**
```swift
import SwiftUI
import SwiftData

/// 伙伴卡片组件
/// 复用: 在 PartnerView 列表中使用
struct PartnerCardView: View {
    let partner: AccountabilityPartner
    var onEdit: () -> Void
    var onDelete: () -> Void
    
    var body: some View {
        HStack(spacing: 12) {
            // 头像
            ZStack {
                Circle()
                    .fill(Color.indigo.opacity(0.2))
                    .frame(width: 50, height: 50)
                
                if let avatarData = partner.avatarData,
                   let uiImage = UIImage(data: avatarData) {
                    Image(uiImage: uiImage)
                        .resizable()
                        .scaledToFill()
                        .frame(width: 50, height: 50)
                        .clipShape(Circle())
                } else {
                    Image(systemName: "person.circle.fill")
                        .font(.system(size: 40))
                        .foregroundStyle(.indigo)
                }
            }
            
            // 信息
            VStack(alignment: .leading, spacing: 4) {
                Text(partner.name)
                    .font(.headline)
                
                HStack(spacing: 4) {
                    Image(systemName: partner.contactType.icon)
                        .font(.caption2)
                        .foregroundStyle(.secondary)
                    Text(partner.contactIdentifier)
                        .font(.caption)
                        .foregroundStyle(.secondary)
                        .lineLimit(1)
                }
                
                if let relationship = partner.relationship {
                    Text(relationship)
                        .font(.caption2)
                        .foregroundStyle(.indigo)
                        .padding(.horizontal, 6)
                        .padding(.vertical, 2)
                        .background(Color.indigo.opacity(0.1))
                        .clipShape(Capsule())
                }
            }
            
            Spacer()
            
            // 验证状态
            Image(systemName: partner.isValidContact ? "checkmark.shield.fill" : "exclamationmark.shield")
                .foregroundStyle(partner.isValidContact ? .green : .orange)
        }
        .padding()
        .background(.ultraThinMaterial)
        .clipShape(RoundedRectangle(cornerRadius: 12))
    }
}
```

**2.4 修改文件: `Views/Partner/PartnerView.swift`**
```swift
import SwiftUI
import SwiftData

struct PartnerView: View {
    
    @Environment(\.modelContext) private var modelContext
    @Query(sort: \AccountabilityPartner.createdAt, order: .reverse) 
    private var partners: [AccountabilityPartner]
    
    @State private var showAddPartner = false
    @State private var showEditPartner = false
    @State private var selectedPartner: AccountabilityPartner?
    
    var body: some View {
        List {
            if partners.isEmpty {
                ContentUnavailableView(
                    "No Partner Yet",
                    systemImage: "person.2.circle",
                    description: Text("Add an accountability partner who will be notified of your progress and when you need support.")
                )
            } else {
                Section("Active Partners") {
                    ForEach(partners) { partner in
                        PartnerCardView(
                            partner: partner,
                            onEdit: { 
                                selectedPartner = partner
                                showEditPartner = true 
                            },
                            onDelete: { deletePartner(partner) }
                        )
                    }
                }
                
                Section("Notification Settings") {
                    NavigationLink("Configure Alerts") {
                        PartnerNotificationSettingsView()
                    }
                }
            }
        }
        .navigationTitle("Accountability Partner")
        .toolbar {
            ToolbarItem(placement: .topBarTrailing) {
                Button {
                    showAddPartner = true
                } label: {
                    Image(systemName: "plus")
                }
            }
        }
        .sheet(isPresented: $showAddPartner) {
            AddPartnerView()
        }
        .sheet(isPresented: $showEditPartner) {
            if let partner = selectedPartner {
                EditPartnerView(partner: partner)
            }
        }
    }
    
    private func deletePartner(_ partner: AccountabilityPartner) {
        modelContext.delete(partner)
    }
}
```

#### 检验标准

**测试用例 1: 联系方式验证**
```swift
func testContactValidation() {
    let phonePartner = AccountabilityPartner(
        name: "Test", 
        contactIdentifier: "+1-555-123-4567",
        contactType: .phone
    )
    assert(phonePartner.isValidContact == true)
    
    let emailPartner = AccountabilityPartner(
        name: "Test",
        contactIdentifier: "test@example.com",
        contactType: .email
    )
    assert(emailPartner.isValidContact == true)
    
    let invalidPartner = AccountabilityPartner(
        name: "Test",
        contactIdentifier: "invalid",
        contactType: .email
    )
    assert(invalidPartner.isValidContact == false)
}
```

---

### 模块 3: Schedule 时间选择器优化

#### 需求描述
将原有的双 Picker 时间选择改为原生 DatePicker，添加智能提醒功能。

#### 生成规则

**3.1 修改文件: `ViewModels/ScheduleViewModel.swift`**
```swift
import SwiftUI
import Combine

@MainActor
final class ScheduleViewModel: ObservableObject {
    
    // MARK: - 时间绑定（替换原有 hour/minute）
    @Published var bedtime: Date {
        didSet { updateSleepDuration() }
    }
    @Published var wakeUpTime: Date {
        didSet { updateSleepDuration() }
    }
    
    // MARK: - 智能提醒（新增）
    @Published var enableWindDownReminder: Bool
    @Published var windDownMinutes: Int  // 睡前提醒提前分钟数
    @Published var enableWakeUpNotification: Bool
    
    // MARK: - 原有属性
    @Published var isHardLock: Bool
    @Published var selectedWeekdays: [Int]
    @Published var isEnabled: Bool
    @Published var sleepDurationText: String = "8h 0m"
    
    private let scheduleManager = ScheduleManager.shared
    private var cancellables = Set<AnyCancellable>()
    
    let weekdayNames = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"]
    
    init() {
        // 从 ScheduleManager 加载并转换为 Date
        let calendar = Calendar.current
        var bedComponents = DateComponents()
        bedComponents.hour = scheduleManager.bedtimeHour
        bedComponents.minute = scheduleManager.bedtimeMinute
        self.bedtime = calendar.date(from: bedComponents) ?? Date()
        
        var wakeComponents = DateComponents()
        wakeComponents.hour = scheduleManager.wakeUpHour
        wakeComponents.minute = scheduleManager.wakeUpMinute
        self.wakeUpTime = calendar.date(from: wakeComponents) ?? Date().addingTimeInterval(28800)
        
        self.isHardLock = scheduleManager.isHardLock
        self.selectedWeekdays = scheduleManager.weekdays
        self.isEnabled = scheduleManager.isEnabled
        
        // 加载提醒设置
        self.enableWindDownReminder = UserDefaults.standard.bool(forKey: "wind_down_reminder_enabled")
        self.windDownMinutes = UserDefaults.standard.integer(forKey: "wind_down_minutes")
        if self.windDownMinutes == 0 { self.windDownMinutes = 30 }
        self.enableWakeUpNotification = UserDefaults.standard.bool(forKey: "wake_up_notification_enabled")
        
        updateSleepDuration()
    }
    
    // MARK: - 公共方法
    
    func saveSchedule() {
        let calendar = Calendar.current
        let bedComponents = calendar.dateComponents([.hour, .minute], from: bedtime)
        let wakeComponents = calendar.dateComponents([.hour, .minute], from: wakeUpTime)
        
        scheduleManager.bedtimeHour = bedComponents.hour ?? 22
        scheduleManager.bedtimeMinute = bedComponents.minute ?? 0
        scheduleManager.wakeUpHour = wakeComponents.hour ?? 6
        scheduleManager.wakeUpMinute = wakeComponents.minute ?? 0
        scheduleManager.isHardLock = isHardLock
        scheduleManager.weekdays = selectedWeekdays
        scheduleManager.isEnabled = isEnabled
        scheduleManager.saveSettings()
        
        // 保存提醒设置
        UserDefaults.standard.set(enableWindDownReminder, forKey: "wind_down_reminder_enabled")
        UserDefaults.standard.set(windDownMinutes, forKey: "wind_down_minutes")
        UserDefaults.standard.set(enableWakeUpNotification, forKey: "wake_up_notification_enabled")
        
        // 重新调度通知
        scheduleNotifications()
    }
    
    func toggleWeekday(_ day: Int) {
        if selectedWeekdays.contains(day) {
            selectedWeekdays.removeAll { $0 == day }
        } else {
            selectedWeekdays.append(day)
            selectedWeekdays.sort()
        }
    }
    
    // MARK: - 私有方法
    
    private func updateSleepDuration() {
        let calendar = Calendar.current
        var components = calendar.dateComponents([.hour, .minute], from: bedtime, to: wakeUpTime)
        
        // 处理跨天情况
        if let hours = components.hour, hours < 0 {
            components.hour = hours + 24
        }
        
        let hours = components.hour ?? 8
        let minutes = components.minute ?? 0
        sleepDurationText = "\(hours)h \(minutes)m"
    }
    
    private func scheduleNotifications() {
        // 使用 NotificationManager 调度提醒
        if enableWindDownReminder {
            let reminderTime = bedtime.addingTimeInterval(Double(-windDownMinutes * 60))
            NotificationManager.shared.scheduleWindDownReminder(at: reminderTime)
        }
        
        if enableWakeUpNotification {
            NotificationManager.shared.scheduleWakeUpNotification(at: wakeUpTime)
        }
    }
}
```

**3.2 修改文件: `Views/Schedule/ScheduleSetupView.swift`**
```swift
import SwiftUI

struct ScheduleSetupView: View {
    
    @StateObject private var viewModel = ScheduleViewModel()
    
    var body: some View {
        NavigationStack {
            Form {
                // MARK: - 时间设置（优化后）
                Section("Sleep Schedule") {
                    DatePicker(
                        "Bedtime",
                        selection: $viewModel.bedtime,
                        displayedComponents: .hourAndMinute
                    )
                    
                    DatePicker(
                        "Wake Up",
                        selection: $viewModel.wakeUpTime,
                        displayedComponents: .hourAndMinute
                    )
                    
                    HStack {
                        Text("Duration")
                        Spacer()
                        Text(viewModel.sleepDurationText)
                            .foregroundStyle(.secondary)
                            .fontWeight(.medium)
                    }
                }
                
                // MARK: - 重复设置
                Section("Repeat") {
                    WeekdaySelector(
                        selectedDays: $viewModel.selectedWeekdays,
                        weekdayNames: viewModel.weekdayNames
                    )
                }
                
                // MARK: - 智能提醒（新增）
                Section("Smart Reminders") {
                    Toggle("Wind Down Reminder", isOn: $viewModel.enableWindDownReminder)
                    
                    if viewModel.enableWindDownReminder {
                        Stepper(
                            "Remind me \(viewModel.windDownMinutes) minutes before",
                            value: $viewModel.windDownMinutes,
                            in: 15...120,
                            step: 15
                        )
                        .foregroundStyle(.secondary)
                    }
                    
                    Toggle("Wake Up Notification", isOn: $viewModel.enableWakeUpNotification)
                }
                
                // MARK: - 锁定模式
                Section("Protection") {
                    Toggle("Hard Lock Mode", isOn: $viewModel.isHardLock)
                    Text("Hard Lock makes it nearly impossible to bypass your bedtime protection.")
                        .font(.caption)
                        .foregroundStyle(.secondary)
                }
                
                // MARK: - 启用开关
                Section {
                    Toggle("Enable Schedule", isOn: $viewModel.isEnabled)
                }
                
                // MARK: - 保存
                Section {
                    Button("Save Schedule") {
                        viewModel.saveSchedule()
                        // 显示成功反馈
                    }
                    .frame(maxWidth: .infinity)
                    .foregroundStyle(.indigo)
                }
            }
            .navigationTitle("Schedule")
        }
    }
}

// MARK: - 复用组件

/// 星期选择器组件
/// 复用: 可在其他需要选择星期的场景使用
struct WeekdaySelector: View {
    @Binding var selectedDays: [Int]
    let weekdayNames: [String]
    
    var body: some View {
        HStack(spacing: 8) {
            ForEach(1...7, id: \.self) { day in
                DayButton(
                    day: day,
                    name: weekdayNames[day - 1],
                    isSelected: selectedDays.contains(day)
                ) {
                    toggleDay(day)
                }
            }
        }
        .frame(maxWidth: .infinity)
        .padding(.vertical, 4)
    }
    
    private func toggleDay(_ day: Int) {
        withAnimation(.spring(duration: 0.3)) {
            if selectedDays.contains(day) {
                selectedDays.removeAll { $0 == day }
            } else {
                selectedDays.append(day)
                selectedDays.sort()
            }
        }
    }
}

struct DayButton: View {
    let day: Int
    let name: String
    let isSelected: Bool
    let action: () -> Void
    
    var body: some View {
        Button(action: action) {
            Text(name)
                .font(.caption)
                .fontWeight(.medium)
                .foregroundStyle(isSelected ? .white : .primary)
                .frame(width: 36, height: 36)
                .background(isSelected ? Color.indigo : Color.gray.opacity(0.2))
                .clipShape(Circle())
        }
        .buttonStyle(.plain)
        .accessibilityLabel("\(name)\(isSelected ? ", selected" : "")")
    }
}
```

#### 废弃代码清理
```swift
// 以下代码将被删除:
// - bedtimeHour: Int
// - bedtimeMinute: Int  
// - wakeUpHour: Int
// - wakeUpMinute: Int
// 以及相关的 Picker 代码块
```

#### 检验标准

**测试用例 1: 时间计算**
```swift
func testSleepDurationCalculation() {
    let viewModel = ScheduleViewModel()
    
    // 设置 22:00 到 06:00
    let calendar = Calendar.current
    viewModel.bedtime = calendar.date(from: DateComponents(hour: 22, minute: 0))!
    viewModel.wakeUpTime = calendar.date(from: DateComponents(hour: 6, minute: 0))!
    
    assert(viewModel.sleepDurationText == "8h 0m")
}
```

---

### 模块 4: Stats 图表增强

#### 需求描述
添加睡眠趋势图表，支持周/月视图切换，增加睡眠质量评分。

#### 生成规则

**4.1 新建文件: `Views/Components/SleepChartView.swift`**
```swift
import SwiftUI
import Charts

/// 睡眠数据图表组件
/// 复用: StatsView 中使用，支持周/月视图
struct SleepChartView: View {
    let records: [SleepRecord]
    let timeRange: TimeRange
    
    enum TimeRange: String, CaseIterable {
        case week = "Week"
        case month = "Month"
        
        var days: Int {
            switch self {
            case .week: return 7
            case .month: return 30
            }
        }
    }
    
    private var filteredRecords: [SleepRecord] {
        let cutoffDate = Calendar.current.date(byAdding: .day, value: -timeRange.days, to: Date())!
        return records.filter { $0.date >= cutoffDate }.sorted { $0.date < $1.date }
    }
    
    private var averageDuration: Double {
        guard !filteredRecords.isEmpty else { return 0 }
        let total = filteredRecords.reduce(0.0) { $0 + $1.durationSeconds }
        return total / Double(filteredRecords.count) / 3600
    }
    
    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            HStack {
                Text("Sleep Duration")
                    .font(.headline)
                
                Spacer()
                
                Text("Avg: \(String(format: "%.1f", averageDuration))h")
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
            }
            
            if filteredRecords.isEmpty {
                ContentUnavailableView(
                    "No Data",
                    systemImage: "chart.line.uptrend.xyaxis",
                    description: Text("Complete more nights to see trends")
                )
                .frame(height: 200)
            } else {
                Chart(filteredRecords) { record in
                    BarMark(
                        x: .value("Date", record.date, unit: .day),
                        y: .value("Hours", record.durationSeconds / 3600)
                    )
                    .foregroundStyle(record.durationSeconds >= 7 * 3600 ? Color.indigo : Color.orange)
                    .cornerRadius(4)
                }
                .frame(height: 200)
                .chartYAxis {
                    AxisMarks(position: .leading)
                }
                .chartXAxis {
                    AxisMarks(values: .stride(by: timeRange == .week ? .day : .weekOfYear)) { value in
                        AxisGridLine()
                        AxisValueLabel(format: .dateTime.weekday(timeRange == .week ? .abbreviated : .narrow))
                    }
                }
            }
        }
        .padding()
        .background(.ultraThinMaterial)
        .clipShape(RoundedRectangle(cornerRadius: 16))
    }
}
```

**4.2 修改文件: `Views/Stats/StatsView.swift`**
```swift
import SwiftUI
import SwiftData
import Charts

struct StatsView: View {
    
    @StateObject private var viewModel = StatsViewModel()
    @Environment(\.modelContext) private var modelContext
    @State private var selectedTimeRange: SleepChartView.TimeRange = .week
    
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 20) {
                    
                    // MARK: - 统计卡片（保留原有）
                    LazyVGrid(columns: [GridItem(.flexible()), GridItem(.flexible())], spacing: 16) {
                        StatCard(
                            title: "Avg Sleep",
                            value: viewModel.formattedAverageSleep,
                            unit: "hrs",
                            icon: "bed.double.fill",
                            color: .indigo
                        )
                        
                        StatCard(
                            title: "Streak",
                            value: "\(viewModel.currentStreak)",
                            unit: "days",
                            icon: "flame.fill",
                            color: .orange
                        )
                        
                        StatCard(
                            title: "Best Streak",
                            value: "\(viewModel.longestStreak)",
                            unit: "days",
                            icon: "trophy.fill",
                            color: .yellow
                        )
                        
                        StatCard(
                            title: "Bypasses",
                            value: "\(viewModel.totalBypassAttempts)",
                            unit: "total",
                            icon: "hand.raised.fill",
                            color: .red
                        )
                    }
                    .padding(.horizontal)
                    
                    // MARK: - 图表（新增）
                    VStack(alignment: .leading, spacing: 12) {
                        Picker("Time Range", selection: $selectedTimeRange) {
                            ForEach(SleepChartView.TimeRange.allCases, id: \.self) { range in
                                Text(range.rawValue).tag(range)
                            }
                        }
                        .pickerStyle(.segmented)
                        .padding(.horizontal)
                        
                        SleepChartView(
                            records: viewModel.recentRecords,
                            timeRange: selectedTimeRange
                        )
                        .padding(.horizontal)
                    }
                    
                    // MARK: - 最近记录（保留原有）
                    VStack(alignment: .leading, spacing: 12) {
                        Text("Recent Nights")
                            .font(.headline)
                            .padding(.horizontal)
                        
                        if viewModel.recentRecords.isEmpty {
                            ContentUnavailableView(
                                "No Records Yet",
                                systemImage: "moon.zzz",
                                description: Text("Complete your first night with NightGuard to see stats.")
                            )
                            .padding()
                        } else {
                            LazyVStack(spacing: 8) {
                                ForEach(viewModel.recentRecords.prefix(7)) { record in
                                    SleepRecordRow(record: record)
                                        .padding(.horizontal)
                                }
                            }
                        }
                    }
                }
                .padding(.vertical)
            }
            .navigationTitle("Stats")
            .onAppear {
                viewModel.loadStats(modelContext: modelContext)
            }
            .refreshable {
                viewModel.loadStats(modelContext: modelContext)
            }
        }
    }
}
```

#### 依赖添加
在 `project.pbxproj` 或 Package.swift 中添加:
```swift
// Charts 框架（iOS 16+ 内置）
import Charts
```

#### 检验标准

**测试用例 1: 图表数据过滤**
```swift
func testChartDataFiltering() {
    let records = [
        SleepRecord(date: Date(), bedtime: Date(), wakeTime: Date(), durationSeconds: 28800),
        SleepRecord(date: Date().addingTimeInterval(-86400), bedtime: Date(), wakeTime: Date(), durationSeconds: 25200),
        SleepRecord(date: Date().addingTimeInterval(-172800), bedtime: Date(), wakeTime: Date(), durationSeconds: 30600)
    ]
    
    let chartView = SleepChartView(records: records, timeRange: .week)
    // 验证图表正确显示最近7天数据
}
```

---

### 模块 5: Accessibility 支持

#### 需求描述
为所有视图添加完整的无障碍支持，包括标签、提示、动态字体适配。

#### 生成规则

**5.1 新建文件: `Extensions/View+Accessibility.swift`**
```swift
import SwiftUI

// MARK: - 无障碍扩展

extension View {
    /// 为按钮添加标准无障碍配置
    func accessibleButton(label: String, hint: String? = nil, traits: AccessibilityTraits = .isButton) -> some View {
        self
            .accessibilityLabel(label)
            .accessibilityHint(hint ?? "")
            .accessibilityAddTraits(traits)
    }
    
    /// 为开关添加无障碍配置
    func accessibleToggle(label: String, value: Bool) -> some View {
        self
            .accessibilityLabel(label)
            .accessibilityValue(value ? "On" : "Off")
            .accessibilityHint("Double tap to toggle")
    }
    
    /// 为图表数据添加无障碍描述
    func accessibleChartValue(title: String, value: String, trend: String? = nil) -> some View {
        self
            .accessibilityLabel("\(title): \(value)")
            .accessibilityHint(trend ?? "")
            .accessibilityAddTraits(.isStaticText)
    }
}

// MARK: - 动态字体支持

extension Font {
    /// 支持动态字体的缩放字体
    static func scalable(_ style: TextStyle, weight: Weight = .regular) -> Font {
        Font.system(style, design: .default, weight: weight)
    }
}
```

**5.2 修改示例: `Views/Shield/ShieldView.swift`**
```swift
// 为主按钮添加无障碍支持
Button {
    if shieldViewModel.isShieldActive {
        shieldViewModel.stopShield()
    } else {
        shieldViewModel.startShieldNow()
    }
} label: {
    Label(
        shieldViewModel.isShieldActive ? "Deactivate Shield" : "Activate Shield",
        systemImage: shieldViewModel.isShieldActive ? "shield.slash" : "shield.fill"
    )
    .font(.headline)
    .frame(maxWidth: .infinity)
    .padding(.vertical, 14)
    .background(shieldViewModel.isShieldActive ? Color.red : Color.green)
    .foregroundStyle(.white)
    .clipShape(RoundedRectangle(cornerRadius: 12))
}
.accessibleButton(
    label: shieldViewModel.isShieldActive ? "Deactivate sleep protection" : "Activate sleep protection",
    hint: shieldViewModel.isShieldActive ? "Turns off app blocking" : "Blocks selected apps until wake time"
)

// 状态指示器
Text(shieldViewModel.isShieldActive ? "Protection Active" : "Protection Off")
    .font(.title)
    .fontWeight(.bold)
    .accessibilityLabel("Protection status: \(shieldViewModel.isShieldActive ? "Active" : "Off")")
```

#### 检验标准

**测试用例 1: VoiceOver 测试**
```swift
func testVoiceOverLabels() {
    // 使用 Xcode Accessibility Inspector 验证:
    // 1. 所有按钮都有明确的 accessibilityLabel
    // 2. 状态变化有 accessibilityNotification
    // 3. 图表数据有语音描述
}
```

---

## 三、实施计划

### 阶段 1: 基础设施（第1-2天）
1. ✅ 创建改进计划文档
2. 🔄 创建 HealthKitService 框架
3. 🔄 创建 PartnerNotificationService

### 阶段 2: 核心功能（第3-5天）
1. 🔄 重构 ScheduleViewModel + ScheduleSetupView
2. 🔄 完善 Partner 模型和视图
3. 🔄 添加 Stats 图表

### 阶段 3: 优化完善（第6-7天）
1. 🔄 添加 Accessibility 支持
2. 🔄 代码清理和重构
3. 🔄 测试验证

### 阶段 4: 废弃代码清理（第8天）
1. 🔄 标记所有废弃代码
2. 🔄 验证无影响后删除
3. 🔄 更新文档

---

## 四、代码清理清单

### 待删除文件/代码

| 位置 | 内容 | 替代方案 | 验证方式 |
|------|------|----------|----------|
| `ScheduleViewModel` | `bedtimeHour`, `bedtimeMinute` | `bedtime: Date` | 测试时间保存/加载 |
| `ScheduleViewModel` | `wakeUpHour`, `wakeUpMinute` | `wakeUpTime: Date` | 测试时间保存/加载 |
| `ScheduleSetupView` | 双 Picker 代码块 | DatePicker | UI 测试 |
| `AccountabilityPartner` | 旧初始化方法 | 新初始化方法 | 数据迁移测试 |

---

## 五、提交规范

### Commit Message 格式
```
[模块] 简短描述

详细说明:
- 修改内容
- 影响范围
- 废弃代码清理范围

Refs: #issue-number
```

### 示例
```
[HealthKit] 添加 HealthKit 集成服务

- 新增 HealthKitService 管理睡眠数据同步
- 修改 SleepRecord 模型添加同步状态字段
- 在 StatsView 添加 HealthKit 连接状态显示

废弃代码清理: 无

Refs: #1
```

---

## 六、风险评估

| 风险 | 可能性 | 影响 | 缓解措施 |
|------|--------|------|----------|
| HealthKit 权限被拒绝 | 中 | 高 | 提供清晰的权限说明，优雅降级 |
| 数据模型迁移问题 | 低 | 高 | 使用 SwiftData 自动迁移 |
| 时间选择器用户不习惯 | 中 | 中 | A/B 测试，保留反馈渠道 |
| Partner 通知被系统限制 | 高 | 中 | 提供本地通知作为备选 |

---

**文档版本**: 1.0  
**最后更新**: 2026-04-28  
**下次评审**: 实施完成后
